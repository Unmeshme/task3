extends Node

#for now nothing again lol
#even gap definition is unnecessary as there is a separation flag
var gap: int = 3

#lets program the default configuration:
var corner_radius: int = 10
var key_size: Vector2 = Vector2(60, 60)
